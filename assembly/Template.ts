import { chain, System, Base64, Base58, Token, Crypto, template, Arrays, StringBytes } from "koinos-sdk-as";

export class Template {
  example(args: template.example_arguments): template.example_result {
    

    return new template.example_result();
  }
}
